import {ReactNode} from "react";

export const Blockgroups = (props: {
    children: ReactNode
}) => {

    // const [groups, setGroups] = useState<>()

    return <>{
        props.children
    }</>
}